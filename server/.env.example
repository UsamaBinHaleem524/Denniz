# Get these from https://dashboard.stripe.com/test/apikeys (use TEST keys while developing)
STRIPE_SECRET_KEY=sk_test_REPLACE_ME

# URL of the frontend (used for CORS + Stripe redirects). Default Vite dev port is 5173.
CLIENT_URL=http://localhost:5173

# Optional — defaults to 4242
PORT=4242
